import React from 'react';
import {StyleSheet, Text, View } from 'react-native';

const Food = ({
    params,
}) => (
    <View>
        <Text>Foods</Text>
    </View>
);

export default Food;
const styles = StyleSheet.create({
    
});